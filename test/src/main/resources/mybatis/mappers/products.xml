<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="products">
	
    <select id="selectAllProducts" resultType="ProductVo">
        SELECT product_no AS productNo, productname, genre, release_date AS releaseDate,
               content, status, img
        FROM products
    </select>
    
    
    <!-- 추가 -->
     <insert id="insertProduct" parameterType="ProductVo">
      	<selectKey keyProperty="productNo" resultType="long"
			order="BEFORE">
			SELECT product_no_seq.nextval FROM dual
		</selectKey>
        INSERT INTO products (product_no ,productname, genre, release_date, content, status, img)
        VALUES (#{productNo} ,#{productName}, #{genre}, #{releaseDate}, #{content}, #{status}, #{img})
    </insert>
    
    <!-- Select product by product number -->
    <select id="getProductdetail" parameterType="long" resultType="ProductVo">
        SELECT product_no AS productNo, productname, genre, release_date AS releaseDate,
               content, status,img
        FROM products
        WHERE product_no = #{productNo}
    </select>
    	<!-- 삭제 -->
	    <delete id="deleteProduct" parameterType="long">
	    DELETE FROM products
	    WHERE product_no = #{productNo}
		</delete>
		
		<!-- 수정 -->
		<update id="modifyProduct" parameterType="ProductVo">
		    UPDATE products
		    SET 
		    productname = #{productName},
		    genre = #{genre},
		    content = #{content},
		    img =#{img}
		    WHERE product_no = #{productNo}
		</update>


    
    <select id="selectProductsByName" parameterType="string" resultType="ProductVo">
        SELECT product_no AS productNo, productname, genre, release_date AS releaseDate,
               content, status, img
        FROM products
        WHERE LOWER(productname) LIKE '%' || LOWER(#{value}) || '%'
    </select>

    <select id="selectProductsByGenre" parameterType="string" resultType="ProductVo">
        SELECT product_no AS productNo, productname, genre, release_date AS releaseDate,
               content, status, img
        FROM products
        WHERE LOWER(genre) LIKE '%' || LOWER(#{value}) || '%'
    </select>

    <!-- Rent a product -->
    <update id="rentProduct">
        UPDATE products
        SET status = '대여중'
        WHERE product_no = #{productNo}
    </update>

    <!-- Return a product -->
    <update id="returnProduct">
        UPDATE products
        SET status = '대여가능'
        WHERE product_no = #{productNo}
    </update>
</mapper>
